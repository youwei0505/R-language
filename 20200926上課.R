x<-c(1,2,5,3)
x^3
x/c(1,2)
#---------------------------------------------------------------
X<-c(1,3,5,3,2,5,6,7,12)
Y<-2+4*X

x<-X-mean(X)
y<-Y-mean(Y)

upper_beta<-sum(x*y)
down_beta<-sum(x^2)

beta_est<-upper_beta/down_beta
#---------------------------------------------------------------
x<-matrix(rnorm(10),nrow=2)
x>0
x[2,3]
(x>=0)*x+(x<=0)*0
#---------------------------------------------------------------
z<-tibble(x=norm(20),y=rep(c(1,2,3,4)),times=5)